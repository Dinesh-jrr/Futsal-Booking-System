name: player
description: "A new Flutter project."
publish_to: 'none'  # Remove this line if you want to publish it

version: 1.0.0+1

environment:
  sdk: ^3.5.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  table_calendar: ^3.0.9
  google_maps_flutter: ^2.5.0
  cached_network_image: ^3.3.0
  socket_io_client: ^2.0.3+1
  google_sign_in: ^6.1.5
  shimmer: ^3.0.0
  another_flushbar: ^1.10.28
  flutter_otp_text_field: ^1.1.1
  image_picker: ^1.0.4

  esewa_flutter_sdk:
    path: C:/Futsal Booking System/Player/external_sdk/esewa_sdk/esewa_flutter_sdk/esewa_flutter_sdk
  geolocator: ^10.1.0
  flutter_rating_bar: ^4.0.1
  flutter_svg: ^1.0.0  # Specify the version for SVG handling
  flutter_bloc: ^8.0.0  # Specify the version for state management
  hydrated_bloc: ^8.0.0  # Specify the version for persistence in bloc
  http: ^1.3.0  # Add this for making HTTP requests to your backend
  shared_preferences: ^2.0.15  # Add this to store data locally (e.g., user login status)
  flutter_secure_storage: ^9.2.4  # Add for securely storing sensitive data (e.g., tokens)
  fluttertoast: ^8.2.12
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  flutter_launcher_icons: ^0.13.1

flutter_icons:
  android: true
  ios: true
  image_path: "assets/images/logo.png"


flutter:
  uses-material-design: true
  assets:
    - assets/fonts/
    - assets/images/
    - assets/vectors/
    - assets/icons/  # Include the icons folder if you use it

  # If you need to include any custom fonts, add them here
  # fonts:
  #   - family: Roboto
  #     fonts:
